@model IEnumerable<NewsMVC.Models.Uye>

@{
    ViewBag.Title = "Üyeler";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}



<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Kullanıcı adı
        </th>
        <th>
            E-mail
        </th>
        <th>
           Şifre
        </th>
        <th>
           Ad Soyad
        </th>
        <th>
            Fotoğraf
        </th>
        <th>
            Yetki
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.KullaniciAdi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sifre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AdSoyad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Resim.KucukBoy)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Yetki.Yetki1)
        </td>
        <td>
            @Html.ActionLink("Düzenle", "Edit", new { id=item.UyeId }) |
            @Html.ActionLink("Detay", "Details", new { id=item.UyeId }) |
            @Html.ActionLink("Sil", "Delete", new { id=item.UyeId })
        </td>
    </tr>
}

</table>
